# Frontend Architecture

Frontend is organized into reusable UI layers:

- `components/`: domain and shared view components.
- `layouts/`: page shell abstractions.
- `hooks/`: reusable state and data hooks.
- `services/`: API clients and DTO mapping.
- `styles/`: design tokens and future theme packs.
